services:
  frontend:
    build:
      context: .
      args:
        VITE_API_URL: ""
    ports:
      - "3000:80"
    depends_on:
      - backend

  backend:
    build:
      context: ./backend
    ports:
      - "3001:8000"
    environment:
      - PYTHONUNBUFFERED=1
